# GoHighLevel Parity — Remaining Roadmap (re-audited 2026-06-22)

Generated from a live-codebase re-audit after v2.56.0. 15 gap items DONE, 34 remaining,
consolidated into 13 epics. Epics #1 (Inbox productivity, v2.55.0) and #2 (Trigger links,
v2.56.0) are SHIPPED. Resume from #3. Each epic: build -> adversarial review -> fix -> tag deploy.

Summary: I validated the audit against the current codebase on branch harden/code-review-remediation. Spot-checks confirm the audit is accurate: no drag-drop/canvas deps in frontend/package.json (no @dnd-kit, @xyflow/react, qrcode, mjml); workflow TRIGGER_TYPES has no webhook.received and the DSL has no goal concept; invoices.service.ts:194 computeTotal is a flat sum with zero tax handling; no Company/companyId, TaxRate, Coupon, CustomerWallet, MessageSnippet, TriggerLink, SendingDomain models exist in schema.prisma (all grep-confirmed absent — the coupon/wallet/snippet grep hits were false positives i

## #1 [yes] Inbox Productivity Suite (snippets + internal notes + bulk actions + CSV export + manual workflow enroll) (SHIPPED)
**Why:** Highest value-per-effort: 5 small, independent, agent-facing wins (4x MISSING, 1x PARTIAL) that compound into daily GHL-grade inbox feel. No new infra, no external deps, mostly composition over existing services. Also unlocks the missing manual workflow-enrollment entrypoint that several later epics reuse.

**Build spec:** MODELS: MessageSnippet{id,workspaceId,ownerId?(null=shared),shortcut @@unique([workspaceId,shortcut]),title,body @db.Text,ts}; ConversationNote{id,workspaceId,conversationId,authorId,body,createdAt; @@index([conversationId,createdAt])} (prefer separate model over a Message authorType=INTERNAL_NOTE so it can never hit sender.send egress). SERVICES: SnippetsService (ws-scoped CRUD, list=shared+own, reuse workflow template-interpolation util for {{contact.firstName}}); ConversationsService.addNote (writes ConversationNote WITHOUT sender.send, streams via ConversationStreamService.push); ExportService (RFC-4180 CSV, no dep, cursor-paged findMany honoring LeadFilterDto). ENDPOINTS on existing controllers: marketing-snippets.controller.ts CRUD (leads.read list / leads.write manage); marketing-conversations.controller.ts @Get(':id/notes')+@Post(':id/notes') (leads.write); @Post('marketing/conversations/bulk'){conversationIds,action:close|reopen|assign|tag|markRead,payload} -> ConversationsService.bulk() capped-batch ws-scoped scopedUpdate; @Post('marketing/leads/bulk-delete')(soft) + @Post('marketing/leads/bulk-enroll'){leadIds,workflowId} -> WorkflowExecutorService.start per lead with origin='manual' (this is the missing manual-enroll entrypoint). FRONTEND: '/'-shortcut autocomplete + snippet manager in InboxPage composer; yellow inline Note toggle in composer; row checkboxes + bulk-action toolbar on ConversationList.tsx and leads list; 'Export CSV' button passing active filters. Confirmed absent: no snippet/note/bulk/export/enroll code in controllers/.

## #2 [yes] Standalone Trigger Links + QR codes (with link.clicked workflow trigger) (SHIPPED)
**Why:** Small, self-contained, real automation value and a recognizable GHL feature. Pairs naturally with the manual-enroll entrypoint from Epic 1 and the workflow trigger additions in Epic 4. Single new dep (qrcode) confirmed absent from package.json.

**Build spec:** MODELS: TriggerLink{id,workspaceId,name,slug(unique-per-ws),targetUrl,createdAt; @@index([workspaceId])}; TriggerLinkClick{id,workspaceId,triggerLinkId,leadId?,ip?,userAgent?,clickedAt; @@index([triggerLinkId,clickedAt])}. SERVICE: TriggerLinksService (ws-scoped CRUD + click(slug,leadId?) records click + emits 'link.clicked' marketing event). DSL: add 'link.clicked' to TRIGGER_TYPES (workflow-dsl.schema.ts:11) + wire into EVENT_FOR_TRIGGER (workflow-trigger.service.ts:10) so workflows fire with filter trigger.triggerLinkId. PUBLIC: @Get('public/l/:slug') resolve slug->targetUrl, record click (resolve lead from signed ?c= contact param appended when sent from campaign/SMS), 302 reusing campaign-tracking's open-redirect-safe pattern. QR: add `qrcode` npm dep; @Get('marketing/trigger-links/:id/qr.png') streams PNG of the public URL. FRONTEND: src/pages/marketing/triggerLinks (CRUD + copy-link + download-QR + click stats).

## #3 [yes] Commerce Money Correctness (taxes + coupons + text-to-pay + wallet/store-credit)
**Why:** Tax-correct invoices (KDV/VAT) are table-stakes for real billing in the Turkish tenant base and the current computeTotal (invoices.service.ts:194) is a flat sum with NO tax line. Coupons drive checkout conversion; text-to-pay is a high-conversion GHL staple that is mostly composition over existing public pay-token pages. Wallet is lower-value but shares the same money-ledger discipline so it rides along cheaply. All confirmed absent (no TaxRate/Coupon/CustomerWallet models).

**Build spec:** TAX: TaxRate{id,workspaceId,name,rate Decimal(5,2),inclusive Bool,default Bool} ws-scoped CRUD mirroring ProductsService. Shared money/tax.util.ts computeTotal -> {subtotal,taxTotal,total, taxSummary[]} grouped by rate; reuse across invoices+estimates+order-forms. Extend InvoiceItem JSON to {description,qty,unitPrice,taxRateId?|taxRatePct?}; persist subtotal+taxTotal on Invoice/Estimate; OrderFormsService.resolveLineItems stamps product.taxRate onto the line; surface breakdown in publicInvoice()/publicView() + Stripe session line-item tax_rates. COUPONS: Coupon{id,workspaceId,code @@unique([workspaceId,code]),kind PERCENT|FIXED,value,currency?,maxRedemptions?,timesRedeemed default 0,startsAt?,expiresAt?,minSubtotal?,active}; CouponRedemption{couponId,invoiceId|orderFormId,leadId,amountOff,redeemedAt}; CouponsService.redeem(ws,code,subtotal) validates active/window/limit/min, returns server-resolved amountOff; atomic increment timesRedeemed in tx guarded timesRedeemed<maxRedemptions; wire into OrderFormsService.submit (optional buyer-supplied code, amount server-resolved) + manual invoice create. TEXT-TO-PAY: InvoicesService.sendByText(ws,invoiceId,leadId) resolves lead SMS/WhatsApp via ChannelAdapterRegistry (same path as CampaignSenderService.send), reserves quota via MessageQuotaService, sends /api/public/i/:token URL; @Post('invoices/:id/text-to-pay'). WALLET: CustomerWallet{id,workspaceId,leadId @@unique([workspaceId,leadId]),balance Int minor,currency}; WalletLedgerEntry{walletId,delta,reason CREDIT|DEBIT|REFUND|MANUAL_ADJUST,invoiceId?,note,createdAt} append-only; balance derived from ledger sum (no lost-update); WalletService.credit/debit in tx with non-negative guard; wire into InvoicesService.pay()/settle so applied credit reduces Stripe amount or settles covered invoice PAID via 'wallet'. FRONTEND: per-line tax picker in InvoiceForm/EstimatesPage + subtotal/tax/total on public pay page; coupons settings + optional code input on public order-form; 'Text to pay' button picking a lead channel; wallet panel on lead detail; TaxRates + Coupons settings pages.

## #4 [yes] Workflow Builder Depth (visual node canvas + goals + drip/sequence template library + inbound-webhook trigger + stats wiring)
**Why:** Signature GHL feel. The execution engine is already strong (durable executor, broad triggers, wait/branch/ai_classify, ScheduledJob resume) — this is almost entirely a builder-UX + thin-config layer over the unchanged executor, so high parity-perception for moderate effort. Inbound-webhook trigger and manual-enroll (Epic 1) together complete the 'sequences' story in Epic 7.

**Build spec:** (1) CANVAS: add @xyflow/react (React Flow) to frontend deps (confirmed absent); AutomationsCanvas renders the existing DSL as nodes (trigger + one node/step), edges from sequential order + branch.elseGoto / ai_classify.routes goto indices (already encoded workflow-dsl.schema.ts:70/85); node panels mutate the SAME steps[] array, serialize back to unchanged POST/PATCH /workflows; keep JSON view as 'advanced' toggle. (2) GOALS: add optional `goal`={Filter set, onMet:'exit'|'goto stepIndex'} to DSL; evaluate in WorkflowExecutorService.advance() before each step via existing handler.matchesAll() (workflow-action.handler.ts:344); short-circuit on match. No new model. (3) TEMPLATE LIBRARY: static workflows/workflow-templates.ts catalog of complete {name,trigger,steps[]} presets (5-email nurture, missed-call text-back, review-request drip) + GET /workflows/templates + 'Start from template' picker pre-filling create(); optionally persist user templates as Workflow status='TEMPLATE'. (4) INBOUND WEBHOOK: add 'webhook.received' to TRIGGER_TYPES; InboundWebhook{id,workspaceId,slug,secret(secret-box),enabled}; public @Post('api/public/hooks/:workspace/:slug') HMAC/secret-header guarded (mirror IngestTokenGuard), appends marketing.webhook.received.v1 outbox event {workspaceId,payload,leadId? resolved by email/phone}; wire into EVENT_FOR_TRIGGER so trigger.* filters resolve against body; admin UI to mint URL+secret. (5) STATS: increment started/completed/failed on Workflow.stats in WorkflowExecutorService.start()/finish() (column exists schema.prisma:1295, never written; list UI reads null today).

## #5 [yes] Opportunity Forecasting + per-stage automation presets
**Why:** Direct depth-gap on the DONE Opportunities item: stage.probability is already persisted but NEVER used. A weighted/expected-revenue forecast is a high-visibility sales-manager number for small effort, and the per-stage automation is just a builder preset over the existing OpportunityStageChanged trigger (no new execution path).

**Build spec:** FORECAST: OpportunitiesService.forecast(ws,pipelineId,user) -> per-stage {count,rawValue,weightedValue=sum(value*stage.probability/100)} + pipeline total weighted/expected value; extend board() stage objects with weightedValue (board() currently sums raw value only, opportunities.service.ts:138). Optional time-bucketed view: add nullable Opportunity.expectedCloseDate (+migration+DTO), group open deals into month buckets. GET marketing/opportunities/forecast (manager-scoped) on marketing-opportunities.controller.ts. PER-STAGE AUTOMATION: deliver as a UX shortcut over the existing engine — in the workflow builder add a 'Pipeline stage entered' trigger preset pre-filling trigger.type=opportunity.stage_changed + a toStageId filter (pipeline+stage picker). All execution stays in WorkflowTriggerService/WorkflowExecutorService (already listens to OpportunityStageChanged, workflow-trigger.service.ts:20). No new cron/secret-box. FRONTEND: forecast strip/card on OpportunitiesPage (weighted total + per-stage bars).

## #6 [yes] Companies / B2B contact-company hierarchy
**Why:** Real B2B/account-based value with native opportunity/conversation rollups that a Custom Object cannot replicate. Self-contained model + service + page; reuses existing CustomFieldDef namespacing and PermissionsGuard patterns. Lead today only has a flat businessName string (schema.prisma:164).

**Build spec:** MODELS: Company{id,workspaceId,name,domain?,phone?,address?,customFields Json,createdAt,updatedAt; @@index([workspaceId])}; add Lead.companyId String? + @@index([workspaceId,companyId]). Reuse CustomFieldDef with entity='COMPANY' (mirror OBJ:<key> namespacing in custom-objects.service.ts). SERVICE: CompaniesService (ws-scoped CRUD + listContacts(companyId) + opportunity/conversation rollup joining Lead.companyId). CONTROLLER: controllers/marketing-companies.controller.ts behind MarketingGuard/PermissionsGuard (contacts.read/write). FRONTEND: src/pages/marketing/companies (list + detail showing linked contacts, opportunities, custom fields) + a company picker on lead detail. Pure code, no external deps.

## #7 [yes] Visual Page/Funnel/Form Builder (drag-drop canvas + field builder + multi-step funnels + template library + popups)
**Why:** Biggest single signature GHL gap and the headline differentiator, but L effort — ranked after the cheaper high-frequency wins so value lands sooner. Crucially the secure server-side renderer (site-renderer.service.ts, JS-free) already consumes a typed block JSON, so the canvas edits the SAME model with no security regression. Custom-domain serving is split out to the needs-external tier (Epic 13).

**Build spec:** PHASE 1 — visual block builder (achievable=yes): React canvas in SitesPage editing the SAME blocks[] the renderer consumes (hero/features/pricing/faq/cta/text/form); add @dnd-kit/core (confirmed absent) for reorder + left component palette + right property panel; serialize to existing PUT /sites/:id. FIELD BUILDER: replace hardcoded createForm fields (name/email/phone, SitesPage.tsx:158) with a visual field list (add/reorder/remove + type picker) writing FormDef.fields; extend renderer allowed input types (site-renderer.service.ts:106 currently only text/email/tel/number) to add select/checkbox/radio/date + matching block render — still plain POST forms. PHASE 2 — multi-step funnels (achievable=yes): add Funnel{id,workspaceId,name,slug,published} + FunnelStep{funnelId,position,sitePageId or inline blocks,redirectToStep} (+migration, ws-scoped) — NOTE the existing funnels/ module is A/B Experiments+Surveys, there is NO Funnel parent model today; FunnelsService.render(ws,slug,stepIndex) reusing SiteRendererService; public route /api/public/funnel/:workspace/:slug/:step?; frontend funnel-flow view reusing the block builder per step. TEMPLATE LIBRARY: SiteTemplate/FunnelTemplate seed rows or static JSON catalog under sites/ + 'Start from template' clone. POPUPS: add `popup` block {trigger:'load'|'exit-intent'|'delay',delaySec,blocks[]} — renderer is intentionally JS-free, so either a CSP-safe inline script behind an opt-in flag OR an always-visible modal section (document the tradeoff).

## #8 [yes] Email Block Builder + EmailTemplate model (shared with campaigns)
**Why:** Branded HTML email is core to GHL marketing parity (campaign body is a raw plain-text Textarea today, sent via sendPlainEmail). High value but L effort and depends on the same dnd canvas built in Epic 7, so it reuses that investment. Block->HTML compile is pure code; only true deliverability needs the sending-domain ESP in Epic 11.

**Build spec:** MODELS: EmailTemplate{id,workspaceId,name,blocks Json (ordered typed: heading/text/image/button/divider/spacer/columns),theme Json,compiledHtml}. RENDERER: emailtemplate.render.ts compiles blocks -> table-based inlined-CSS HTML (mjml or hand-rolled table layout; inline styles for client compat) mirroring SiteRendererService; click-tracking rewrite operates on hrefs. CAMPAIGN: add bodyHtml/templateId; CampaignSenderService EMAIL branch switches from email.sendPlainEmail to a new generic EmailService.sendHtml(to,subject,html) (transporter already supports html:). FRONTEND: drag-drop email-block editor reusing the Epic-7 @dnd-kit canvas, live preview iframe, template library CRUD; keep the existing Textarea as a 'plain text' fallback mode.

## #9 [yes] Email Marketing Depth (A/B split testing + drip/sequence packaging + list-hygiene tier-1)
**Why:** Three complementary email features that all sit on existing infra: A/B is pure code over CampaignRecipient/tracking; drip 'sequences' are a thin packaging layer over the already-complete workflow executor (engine done — gap is bulk-enroll UX, which Epic 1 + Epic 4 already provide); list-hygiene tier-1 (syntax/MX + bounce suppression) is pure code. Real-time verification API and ESP bounce feedback are the only needs-external slivers (deferred).

**Build spec:** A/B: CampaignVariant{id,campaignId,workspaceId,key A|B|..,weight Int,subject,body/bodyHtml|templateId,stats Json} + Campaign.abMode/abTestPercent/abWinnerMetric(OPEN|CLICK); CampaignsService.launch assigns each frozen CampaignRecipient a variantKey (weighted random, +column); CampaignSenderService.batch picks variant subject/body; optional winner phase via ScheduledJob computing winner from campaign-tracking opens/clicks (openedAt/clickedAt already recorded) then sends winner to remainder. DRIP/SEQUENCES (packaging over existing executor — wait step supports duration 60s..30d + until_reply, schema lines 88-94): SequenceEnrollment path calling WorkflowExecutorService.start per lead matching a buildAudienceWhere filter (bulk-enroll already added in Epic 1) + bulk ScheduledJob to throttle; ensure conversation.message.received cancels until_reply runs; optional send-window/business-hours guard in send_* handling; frontend 'Sequences' view listing email-centric workflows + 'Enroll leads' audience UI + per-workflow run stats. LIST HYGIENE (tier-1 pure code): add lead.emailVerifiedStatus(UNKNOWN|VALID|INVALID|RISKY)+emailBouncedAt; verifyEmailSyntaxMx() (regex+dns.resolveMx) at ingest, exclude INVALID from campaign audience; ESP bounce/complaint webhook controller (mirror email-webhook.controller.ts) flips emailBouncedAt + sets emailOptOut on hard bounce (CampaignSenderService.isOptedOut already gates). [tier-2 ZeroBounce/NeverBounce/Kickbox real-time API = needs-external, deferred].

## #10 [yes] Memberships / Courses Depth (lesson drip + completion certificates + gamification)
**Why:** Three standard course-platform features for the community+course product, all pure code over the existing EnrollmentService/Lesson/Communities models. Ranked here because the membership product is a narrower audience than core CRM/marketing, but each item is independently shippable S-M effort. Certificate PDF reuses the existing documents/ e-sign PDF pipeline.

**Build spec:** DRIP/GATING: add Lesson.dripDays Int? + Lesson.gating enum FREE|SEQUENTIAL|DRIP + Course.dripMode; EnrollmentService.resolveAccess(enrollment,lesson) locks DRIP if (now-enrolledAt)<dripDays or SEQUENTIAL if prior-position lesson not completed; getProgress returns each lesson {locked,unlockAt}; guard markLessonComplete to 403 locked; optional @Cron 'drip-unlock' emitting 'lesson unlocked' event. CERTIFICATES: Course.certificateEnabled + certificateTemplate(title/signature/logo Json); Certificate{id,workspaceId,enrollmentId @unique,leadId,courseId,serial @unique,issuedAt,pdfUrl?}; in markLessonComplete when pct>=100 && certificateEnabled, create Certificate + emit marketing event for a 'certificate issued' workflow trigger; certificate.service.ts renders HTML->PDF (reuse documents/ pipeline) stored under UPLOADS_DIR (mirror branding.saveLogo); public verify /api/public/certificates/:serial. GAMIFICATION: PointsLedger{id,workspaceId,leadId,source LESSON_COMPLETE|POST_CREATED|COMMENT|COURSE_COMPLETE,points,refId,createdAt} append-only; Badge{workspaceId,key,name,iconUrl,ruleType,threshold} + EarnedBadge{leadId,badgeId @@unique,earnedAt}; gamification.service.ts award() called from markLessonComplete + Communities post/comment, evaluates badge rules idempotently; leaderboard(ws)=grouped sum, paginated; ws-scoped controller + public member read. FRONTEND: course-editor drip/certificate fields + lock badges + unlock dates; leaderboard table + badge shelf + certificate download/print.

## #11 [yes] Affiliate Public Portal + Preview Dialer (achievable agency/voice wins)
**Why:** Two otherwise-orphaned pure-code items grouped as a tail batch. Affiliate engine is already DONE internally (affiliate.service.ts) — only the public self-serve portal is missing, which is real agency value via a token guard mirroring IngestTokenGuard. A PREVIEW dialer (queue + click-to-dial + auto-advance) is fully buildable on the existing single-line click-to-dial path; only TRUE parallel power-dialing needs NetGSM trunk channels (split out as inert).

**Build spec:** AFFILIATE PORTAL: add Affiliate.portalTokenHash (sha256, like IngestTokenGuard) + lastLoginAt; AffiliatePortalGuard resolves affiliate from bearer/magic-link token -> req.affiliate; public controller (@MarketingRoute, throttled): GET /public/affiliate/me (profile+code+link), GET /public/affiliate/referrals + /commissions (own rows only, reuse AffiliateService.listReferrals/listCommissions scoped to affiliate.id), GET /public/r/:code redirect calling recordReferral + dropping attribution cookie/lead source; optional public self-signup creating PENDING affiliate for staff approval; standalone affiliate portal frontend (stats + referral link + payout history); all writes workspace+affiliate scoped. PREVIEW DIALER (no new telephony infra): DialSession{workspaceId,marketingUserId,status,currentIndex} + DialSessionItem{sessionId,leadId,position,outcome} seeded from a saved lead segment/filter; dialer.service.ts createSession(leadFilter) materializes queue, next(sessionId) returns next lead + calls existing SalesCallService.startCall (click-to-dial), logCall auto-advances; reuse single-line concurrency guard (maxConcurrentCalls=1); controller POST sessions / GET sessions/:id/next, mirror outcomes onto lead timeline; voice/ dialer frontend (queue progress + dial/skip/log + auto-advance). NOTE: true parallel power dialer additionally needs an api-dial NetGSM adapter with maxConcurrentCalls>1 = needs-external (Epic 13).

## #12 [needs-external] Calendar + Social Breadth (Outlook/O365 2-way sync + X/Twitter + Pinterest + Google Business Profile posting)
**Why:** High-parity but each requires a third-party OAuth app/credentials the engineer cannot self-provision (Azure AD app; paid X API v2 write ~$100+/mo; Pinterest dev app; allowlisted Google Business Profile API). All four mirror an existing proven pattern (Google Calendar connection / social network-adapters), so the app-layer code is buildable now and stays inert until an operator connects the account. Ranked after all pure-code work.

**Build spec:** OUTLOOK (mirror Google Calendar exactly): OutlookCalendarConnection{id,workspaceId,marketingUserId,outlookCalendarId @default('primary'),accessToken(SEALED),refreshToken(SEALED),tokenExpiresAt,deltaToken?,subscriptionId?/clientState?/subscriptionExpiration?,enabled,ts; @@index([workspaceId])}; integrations/outlook-calendar.service.ts (env-gated MS_OAUTH_CLIENT_ID/_SECRET + secret-box, AES-GCM sealed state, SSRF-safe safeFetch, refresh skew; authorize login.microsoftonline.com/common/oauth2/v2.0/authorize, scope Calendars.ReadWrite offline_access); outlook-calendar-sync.service.ts (Graph /me/events delta pull + push new Bookings via POST /me/events; ScheduledJob renewal mirroring Google watch-channel); outlook-calendar.controller.ts (auth-url + admin connect/disconnect + public callback coarse error codes); frontend settings/connections/OutlookCalendarTab.tsx. INERT until Azure AD app + MS_OAUTH creds. SOCIAL (add to NETWORKS const + Network enum + frontend SOCIAL_NETWORKS — all currently FACEBOOK/INSTAGRAM/LINKEDIN/TIKTOK): TWITTER publishTwitter() POST api.twitter.com/2/tweets (OAuth2 PKCE token sealed via openSecret, media via v1.1 upload), branch in isNetworkConfigured (X_CLIENT_ID/_SECRET) — INERT without paid X app. PINTEREST publishPinterest() POST api.pinterest.com/v5/pins (board_id in SocialAccount.externalId), isNetworkConfigured (PINTEREST_APP_ID/_SECRET) — INERT without Pinterest app. GMB publishGmb() POST mybusiness.googleapis.com/v4/.../localPosts (reuse GoogleCalendarConnection OAuth/refresh pattern, location id in SocialAccount.externalId, business.manage scope) — INERT until Google approves Business Profile API; build Local Posts only (GMB messaging sunset).

## #13 [needs-external] Deliverability + PSP + Reputation + White-label Infra (sending domains/DKIM, non-Stripe PSP, review-sync, custom-domain white-label, call-recording, prospecting audit)
**Why:** Heaviest infra-dependent cluster: each needs an external account (transactional ESP, PayTR/Iyzico merchant, Google Business/FB tokens, NetGSM recording, Google PSI key) and/or edge infra (wildcard TLS + reverse proxy for custom domains) that cannot be provisioned in-repo. Build the app-layer model + verification + adapter now; cutover requires ops. Ranked last as lowest value-per-effort given the inert tail.

**Build spec:** SENDING DOMAINS + DKIM/SPF/DMARC: SendingDomain{id,workspaceId,domain,status PENDING|VERIFIED|FAILED,dkimSelector,dkimPublicKey,spfExpected,dmarcExpected,verifiedAt,providerDomainId}; integrate ESP (Postmark/SendGrid/Mailgun/SES): SendingDomainsService registers domain + stores DNS records + verify() polls ESP or dns.resolveTxt; ScheduledJob 'sending-domain.verify' re-polls PENDING (ScheduledJobRunner pattern); route campaign EMAIL through workspace VERIFIED domain (per-ws transporter/From), fallback to platform default; settings page with copy-able DNS + Verify button. INERT without ESP + tenant DNS. NON-STRIPE PSP: generalize workspace PSP into adapter registry mirroring modules/billing BillingPaymentProvider — WorkspacePaymentProvider port (id 'stripe'|'paytr'|'iyzico'|'manual', supports(currency), createCheckout(invoice,ctx)->{redirect|iframe|bank_transfer}, verify(payload)->settled); refactor InvoicesService.pay() to resolve provider from WorkspacePspConfig.provider instead of inline STRIPE branch; add PaytrProvider (reuse hash/init-iframe shape from modules/billing/payments/paytr.provider.ts, seal merchant_id/key/salt via secret-box like setPspConfig); per-ws PayTR webhook/return controller calling shared settle()->InvoicePaid; extend PSP-config DTO + settings UI. INERT until tenant connects a PayTR/Iyzico merchant. REVIEW-SYNC (Google/FB): extend ReviewSource{placeId?,accessToken?(sealed),lastSyncedAt?,externalRef}; Review{externalReviewId? unique-per-source,source GOOGLE|FACEBOOK|GATE,authoredAt}; review-sync.service.ts modeling AdsPullService (@Cron EVERY_HOUR, withAdvisoryLock('reviews:sync'), env-gated, cross-ws DUE-source read whitelisted in scoping fitness test, idempotent upsert on externalReviewId, markError); google-business.client.ts (accounts.locations.reviews.list) + facebook-reviews.client.ts (Graph /{page}/ratings); OAuth connect endpoints (sealed tokens); emit ReviewReceived for low-rating -> workflow trigger; ReviewsPage source-connect + unified synced-review inbox. INERT without Google Business/FB page tokens. CUSTOM-DOMAIN WHITE-LABEL: CustomDomain{workspaceId,hostname @unique,verifyToken,status PENDING|VERIFIED|ACTIVE,txtVerifiedAt,sslStatus}; domains.service.ts request(hostname) returns CNAME+TXT instructions, @Cron verify sweep (advisory-locked) flips VERIFIED; Host-header middleware maps req.host->workspaceId->sites.renderPublic(resolvedWs, slug or '/'); CRUD + status controller; domains settings page. INERT until ops wires wildcard ingress + on-demand TLS (Caddy/ACME). CALL-RECORDING RETRIEVAL: declare 'recording' capability on NetgsmApiAdapter; recording-sync.service.ts @Cron (advisory-locked, env-gated, mirror AdsPullService) for ended SalesCalls with externalCallId + null recordingUrl -> NetGSM CDR/recording endpoint -> stamp SalesCall.recordingUrl (column exists schema.prisma:719, currently never written); ws-scoped signed GET marketing/calls/:id/recording (REP own / manager any); CallsPage play button when present. INERT until NetGSM enables recording + exposes download API. PROSPECTING AUDIT: ProspectAudit{workspaceId,targetUrl,businessName,status,score Int,sections Json,reportUrl,createdAt}; prospecting/audit.service.ts fetches PageSpeed Insights (free Google API key) + on-page checks (title/meta/SSL via server fetch) + ReviewSource presence, async via ScheduledJob, branded PDF (documents/ + branding); public /api/public/audits/:token + 'convert audit->lead'; prospecting frontend. INERT without PSI key. DEFER ENTIRELY: Yext-style listings management + true parallel power dialer — both fundamentally require a paid aggregator/trunk-channel contract and stay fully inert; not worth the L effort until those accounts exist.
