# Ana Ekran Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/home` ekranını iki sütuna böl — solda %38 sekmeli takvim/akış, sağda %62 sohbet — ve sidebar'ı 15 hub'dan 3 yüzey + ayarlara indir.

**Architecture:** Üç bölmenin ikisi zaten var: `CommandBar` (sohbet) ve `AgentActivity` (akış). Yeni olan tek şey takvim: dört kaynağı (cron `nextAt`, `MarketingTask.dueDate`, `Booking.startAt`, kampanya tarihleri) sunucuda birleştiren `GET /marketing/home/timeline` ucu. Her kaynak bağımsız başarısız olur ve okunamayan kaynak **adıyla** raporlanır.

**Tech Stack:** NestJS + Prisma (backend), Vite + React + TanStack Query + vitest (frontend), Jest (backend).

Spec: `docs/superpowers/specs/2026-08-28-panel-ia-design.md`

---

## Dosya yapısı

**Backend — yeni:**
- `src/modules/marketing/analytics/home-timeline.service.ts` — dört kaynağı birleştirir
- `src/modules/marketing/analytics/home-timeline.service.spec.ts`
- `test/e2e/home-timeline.realdb.e2e-spec.ts`

**Backend — değişen:**
- `src/modules/marketing/controllers/marketing-ai.controller.ts` — timeline ucu buraya değil, ayrı controller'a gider (aşağıya bak)
- `src/modules/marketing/controllers/marketing-home.controller.ts` — **yeni**
- `src/modules/marketing/marketing.module.ts` — servis + controller kaydı

**Frontend — yeni:**
- `src/features/marketing/api/homeTimeline.service.ts` — istemci + tipler
- `src/pages/marketing/command/TimelinePanel.tsx` — takvim bölmesi
- `src/pages/marketing/command/TimelinePanel.test.tsx`
- `src/pages/marketing/command/LeftColumn.tsx` — sekmeli sarmalayıcı + rozet
- `src/pages/marketing/command/LeftColumn.test.tsx`

**Frontend — değişen:**
- `src/pages/marketing/command/CommandCenterPage.tsx` — iki sütun düzeni
- `src/features/marketing/navigation.ts` — 15 hub → 3 + ayarlar
- `src/features/marketing/navigation.test.ts` — yeni yapıya göre

---

### Task 1: Timeline servisi — dört kaynak, bağımsız başarısızlık

**Files:**
- Create: `backend/src/modules/marketing/analytics/home-timeline.service.ts`
- Test: `backend/src/modules/marketing/analytics/home-timeline.service.spec.ts`

- [ ] **Step 1: Write the failing test**

`backend/src/modules/marketing/analytics/home-timeline.service.spec.ts`:

```ts
import { HomeTimelineService } from './home-timeline.service';

const WS = 'ws-1';
const FROM = new Date('2026-08-28T00:00:00Z');
const TO = new Date('2026-08-29T00:00:00Z');

function make(over: Partial<Record<string, unknown>> = {}) {
  const prisma = {
    marketingTask: { findMany: jest.fn().mockResolvedValue([]) },
    booking: { findMany: jest.fn().mockResolvedValue([]) },
    socialCampaign: { findMany: jest.fn().mockResolvedValue([]) },
    campaign: { findMany: jest.fn().mockResolvedValue([]) },
    ...over,
  } as never;
  const jobs = { listCronHeartbeats: jest.fn().mockResolvedValue({ registered: [], recorded: [] }) };
  return { svc: new HomeTimelineService(prisma, jobs as never), prisma, jobs };
}

describe('HomeTimelineService', () => {
  it('merges four sources onto one axis, sorted by time, each tagged with its kind', async () => {
    const { svc, prisma, jobs } = make({
      marketingTask: {
        findMany: jest.fn().mockResolvedValue([
          { id: 't1', title: 'Hasan Usta ara', dueDate: new Date('2026-08-28T09:00:00Z') },
        ]),
      },
      booking: {
        findMany: jest.fn().mockResolvedValue([
          { id: 'b1', name: 'Demo', startAt: new Date('2026-08-28T14:00:00Z') },
        ]),
      },
      campaign: {
        findMany: jest.fn().mockResolvedValue([
          { id: 'c1', name: 'Eylül maili', scheduledAt: new Date('2026-08-28T18:00:00Z') },
        ]),
      },
    });
    jobs.listCronHeartbeats.mockResolvedValue({
      registered: [{ name: 'research-nightly', nextAt: new Date('2026-08-28T03:00:00Z') }],
      recorded: [],
    });

    const out = await svc.timeline(WS, FROM, TO);

    expect(out.items.map((i) => i.kind)).toEqual(['system', 'task', 'appointment', 'campaign']);
    expect(out.items[0].title).toBe('research-nightly');
    expect(out.items[1].title).toBe('Hasan Usta ara');
    expect(out.unread).toEqual([]);
  });

  it('names a source it could not read instead of returning a short list silently', async () => {
    const { svc } = make({
      marketingTask: { findMany: jest.fn().mockRejectedValue(new Error('boom')) },
      booking: {
        findMany: jest.fn().mockResolvedValue([
          { id: 'b1', name: 'Demo', startAt: new Date('2026-08-28T14:00:00Z') },
        ]),
      },
    });

    const out = await svc.timeline(WS, FROM, TO);

    // The surviving source still arrives...
    expect(out.items).toHaveLength(1);
    // ...and the missing one is named, because a short list and a broken query
    // look identical otherwise.
    expect(out.unread).toEqual(['görevler']);
  });

  it('scopes every source to the workspace', async () => {
    const { svc, prisma } = make();
    await svc.timeline(WS, FROM, TO);
    for (const model of ['marketingTask', 'booking', 'socialCampaign', 'campaign'] as const) {
      const arg = (prisma as never as Record<string, { findMany: jest.Mock }>)[model].findMany.mock
        .calls[0][0];
      expect(arg.where.workspaceId).toBe(WS);
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && npx jest src/modules/marketing/analytics/home-timeline.service.spec.ts`
Expected: FAIL — `Cannot find module './home-timeline.service'`

- [ ] **Step 3: Write minimal implementation**

`backend/src/modules/marketing/analytics/home-timeline.service.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { PrismaService } from '../../../prisma/prisma.service';
import { ScheduledJobService } from '../scheduling/scheduled-job.service';

export type TimelineKind = 'system' | 'task' | 'appointment' | 'campaign';

export interface TimelineItem {
  kind: TimelineKind;
  at: string;
  title: string;
  id: string;
  status?: string;
}

export interface HomeTimeline {
  from: string;
  to: string;
  items: TimelineItem[];
  /** Sources that could not be read, by name. Empty when all four answered. */
  unread: string[];
}

/**
 * The home screen's calendar: four sources on one time axis.
 *
 * Each source is read independently and a failure NAMES itself rather than
 * shrinking the list. A short calendar and a broken query look identical to a
 * reader, and this codebase has already paid for that once — the daily brief
 * swallowed eight queries into `catch(() => 0)` and reported "nothing to
 * report" for what was actually "the query threw" (fixed in v2.271.0).
 */
@Injectable()
export class HomeTimelineService {
  private readonly logger = new Logger(HomeTimelineService.name);

  constructor(
    private readonly prisma: PrismaService,
    private readonly jobs: ScheduledJobService,
  ) {}

  async timeline(workspaceId: string, from: Date, to: Date): Promise<HomeTimeline> {
    const unread: string[] = [];
    const soft = <T>(label: string, fallback: T) => (e: unknown): T => {
      unread.push(label);
      this.logger.warn(`timeline source "${label}" failed for ${workspaceId}: ${e instanceof Error ? e.message : e}`);
      return fallback;
    };

    const [system, tasks, bookings, socials, campaigns] = await Promise.all([
      this.jobs
        .listCronHeartbeats()
        .then((r) => r.registered)
        .catch(soft('sistem işleri', [] as Array<{ name: string; nextAt: Date | null }>)),
      this.prisma.marketingTask
        .findMany({
          where: { workspaceId, dueDate: { gte: from, lte: to }, status: { in: ['PENDING', 'IN_PROGRESS'] } },
          select: { id: true, title: true, dueDate: true, status: true },
        })
        .catch(soft('görevler', [])),
      this.prisma.booking
        .findMany({
          where: { workspaceId, startAt: { gte: from, lte: to }, status: 'CONFIRMED' },
          select: { id: true, name: true, startAt: true },
        })
        .catch(soft('randevular', [])),
      this.prisma.socialCampaign
        .findMany({
          where: { workspaceId, startDate: { gte: from, lte: to } },
          select: { id: true, name: true, startDate: true, status: true },
        })
        .catch(soft('sosyal kampanyalar', [])),
      this.prisma.campaign
        .findMany({
          where: { workspaceId, scheduledAt: { gte: from, lte: to } },
          select: { id: true, name: true, scheduledAt: true, status: true },
        })
        .catch(soft('kampanyalar', [])),
    ]);

    const items: TimelineItem[] = [
      ...system
        .filter((c) => c.nextAt && c.nextAt >= from && c.nextAt <= to)
        .map((c) => ({ kind: 'system' as const, id: c.name, title: c.name, at: c.nextAt!.toISOString() })),
      ...tasks.map((t) => ({ kind: 'task' as const, id: t.id, title: t.title, at: t.dueDate.toISOString(), status: t.status })),
      ...bookings.map((b) => ({ kind: 'appointment' as const, id: b.id, title: b.name, at: b.startAt.toISOString() })),
      ...socials.map((s) => ({ kind: 'campaign' as const, id: s.id, title: s.name, at: s.startDate.toISOString(), status: String(s.status) })),
      ...campaigns.map((c) => ({ kind: 'campaign' as const, id: c.id, title: c.name, at: c.scheduledAt!.toISOString(), status: c.status })),
    ].sort((a, b) => a.at.localeCompare(b.at));

    return { from: from.toISOString(), to: to.toISOString(), items, unread };
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && npx jest src/modules/marketing/analytics/home-timeline.service.spec.ts`
Expected: PASS, 3 tests

- [ ] **Step 5: Commit**

```bash
git add backend/src/modules/marketing/analytics/home-timeline.service.ts backend/src/modules/marketing/analytics/home-timeline.service.spec.ts
git commit -m "feat(home): merge four calendar sources onto one axis, naming any that fail"
```

---

### Task 2: REST ucu

**Files:**
- Create: `backend/src/modules/marketing/controllers/marketing-home.controller.ts`
- Modify: `backend/src/modules/marketing/marketing.module.ts`

- [ ] **Step 1: Write the controller**

`backend/src/modules/marketing/controllers/marketing-home.controller.ts`:

```ts
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { MarketingGuard } from '../guards/marketing.guard';
import { CurrentMarketingUser } from '../decorators/current-marketing-user.decorator';
import type { MarketingUserPayload } from '../types';
import { HomeTimelineService } from '../analytics/home-timeline.service';

/** Default window: now → +7 days. Long enough that a weekly rhythm is visible,
 *  short enough that the panel is not a wall. */
const DEFAULT_DAYS = 7;

@Controller('marketing/home')
@UseGuards(MarketingGuard)
export class MarketingHomeController {
  constructor(private readonly timeline: HomeTimelineService) {}

  @Get('timeline')
  run(
    @CurrentMarketingUser() actor: MarketingUserPayload,
    @Query('from') from?: string,
    @Query('to') to?: string,
  ) {
    const start = from && !Number.isNaN(Date.parse(from)) ? new Date(from) : new Date();
    const end =
      to && !Number.isNaN(Date.parse(to))
        ? new Date(to)
        : new Date(start.getTime() + DEFAULT_DAYS * 86_400_000);
    return this.timeline.timeline(actor.workspaceId, start, end);
  }
}
```

- [ ] **Step 2: Register in the module**

`backend/src/modules/marketing/marketing.module.ts` — add to imports, `controllers` and `providers`:

```ts
import { MarketingHomeController } from './controllers/marketing-home.controller';
import { HomeTimelineService } from './analytics/home-timeline.service';
```

Add `MarketingHomeController` to the `controllers` array and `HomeTimelineService` to `providers`.

- [ ] **Step 3: Verify it compiles and the app boots**

Run: `cd backend && npx tsc --noEmit -p tsconfig.json && npx jest src/modules/marketing/analytics`
Expected: no type errors; timeline tests still PASS

- [ ] **Step 4: Commit**

```bash
git add backend/src/modules/marketing/controllers/marketing-home.controller.ts backend/src/modules/marketing/marketing.module.ts
git commit -m "feat(home): expose GET /marketing/home/timeline"
```

---

### Task 3: Gerçek-DB e2e — sorgu geçerliliği ve kiracı izolasyonu

**Files:**
- Create: `backend/test/e2e/home-timeline.realdb.e2e-spec.ts`

Birim testleri Prisma'yı mock'lar ve mock her `where`'i kabul eder. `workspaceId: { in: [id, null] }` sekiz hafta boyunca yeşil bir paketle fırlattı. Bu yüzden gerçek Postgres şart.

- [ ] **Step 1: Write the failing test**

```ts
import { randomUUID } from 'crypto';
import { NestExpressApplication } from '@nestjs/platform-express';
import { PrismaService } from '../../src/prisma/prisma.service';
import { HomeTimelineService } from '../../src/modules/marketing/analytics/home-timeline.service';
import { createRealDbTestApp, closeTestApp, realDbEnabled } from '../utils/test-app';

const describeRealDb = realDbEnabled() ? describe : describe.skip;

describeRealDb('Home timeline — real DB (e2e)', () => {
  let app: NestExpressApplication;
  let prisma: PrismaService;
  let svc: HomeTimelineService;

  const workspaceId = randomUUID();
  const otherWorkspaceId = randomUUID();
  const from = new Date('2026-08-28T00:00:00Z');
  const to = new Date('2026-09-04T00:00:00Z');

  beforeAll(async () => {
    ({ app, prisma } = await createRealDbTestApp());
    svc = app.get(HomeTimelineService);

    for (const id of [workspaceId, otherWorkspaceId]) {
      await prisma.workspace.create({
        data: { id, slug: `tl-${id.slice(0, 8)}`, name: 'TL', productName: 'TL' } as never,
      });
    }
    await prisma.marketingTask.create({
      data: {
        workspaceId,
        title: 'Bizim görev',
        dueDate: new Date('2026-08-29T09:00:00Z'),
        status: 'PENDING',
      } as never,
    });
    await prisma.marketingTask.create({
      data: {
        workspaceId: otherWorkspaceId,
        title: 'Yabancı görev',
        dueDate: new Date('2026-08-29T10:00:00Z'),
        status: 'PENDING',
      } as never,
    });
  });

  afterAll(async () => {
    await prisma.marketingTask.deleteMany({ where: { workspaceId: { in: [workspaceId, otherWorkspaceId] } } });
    await prisma.workspace.deleteMany({ where: { id: { in: [workspaceId, otherWorkspaceId] } } });
    await closeTestApp(app);
  });

  it('executes every source query against real Postgres', async () => {
    const out = await svc.timeline(workspaceId, from, to);
    // A rejected query would land here as a named source, not as a throw.
    expect(out.unread).toEqual([]);
  });

  it('never returns another tenant’s row', async () => {
    const out = await svc.timeline(workspaceId, from, to);
    const titles = out.items.map((i) => i.title);
    expect(titles).toContain('Bizim görev');
    expect(titles).not.toContain('Yabancı görev');
  });
});
```

- [ ] **Step 2: Start the test database and run**

```bash
docker start kds-marketing-postgres
cd backend && npx prisma migrate deploy
E2E_REAL_DB=1 npx jest --config test/jest-e2e.json home-timeline.realdb --runInBand
```

Expected: PASS, 2 tests. (`--runInBand` şart: her paket tam bir Nest uygulaması ayağa kaldırıyor, paralel koşarken birbirlerine giriyorlar.)

- [ ] **Step 3: Commit**

```bash
git add backend/test/e2e/home-timeline.realdb.e2e-spec.ts
git commit -m "test(home): prove the timeline queries execute and stay in one tenant"
```

---

### Task 4: Frontend istemci

**Files:**
- Create: `frontend/src/features/marketing/api/homeTimeline.service.ts`

- [ ] **Step 1: Write the client**

```ts
import marketingApi from './marketingApi';

export type TimelineKind = 'system' | 'task' | 'appointment' | 'campaign';

export interface TimelineItem {
  kind: TimelineKind;
  at: string;
  title: string;
  id: string;
  status?: string;
}

export interface HomeTimeline {
  from: string;
  to: string;
  items: TimelineItem[];
  unread: string[];
}

export const getHomeTimeline = (): Promise<HomeTimeline> =>
  marketingApi.get<HomeTimeline>('/home/timeline').then((r: { data: HomeTimeline }) => r.data);
```

- [ ] **Step 2: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/features/marketing/api/homeTimeline.service.ts
git commit -m "feat(home): timeline api client"
```

---

### Task 5: Takvim bölmesi

**Files:**
- Create: `frontend/src/pages/marketing/command/TimelinePanel.tsx`
- Test: `frontend/src/pages/marketing/command/TimelinePanel.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { render, screen } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { TimelinePanel } from './TimelinePanel';
import * as api from '../../../features/marketing/api/homeTimeline.service';

vi.mock('react-i18next', () => ({ useTranslation: () => ({ t: (k: string) => k }) }));

const wrap = (ui: React.ReactNode) => {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>);
};

describe('TimelinePanel', () => {
  it('separates machine work from human work', async () => {
    vi.spyOn(api, 'getHomeTimeline').mockResolvedValue({
      from: '', to: '', unread: [],
      items: [
        { kind: 'system', id: 'research-nightly', title: 'research-nightly', at: '2026-08-29T03:00:00Z' },
        { kind: 'task', id: 't1', title: 'Hasan Usta ara', at: '2026-08-29T09:00:00Z' },
      ],
    });

    wrap(<TimelinePanel />);

    expect(await screen.findByText('Hasan Usta ara')).toBeInTheDocument();
    // Machine work is present but visually recessive — the marker carries it.
    expect(screen.getByTestId('tl-system-research-nightly')).toHaveAttribute('data-kind', 'system');
    expect(screen.getByTestId('tl-task-t1')).toHaveAttribute('data-kind', 'task');
  });

  it('names a source it could not read', async () => {
    vi.spyOn(api, 'getHomeTimeline').mockResolvedValue({
      from: '', to: '', items: [], unread: ['görevler'],
    });

    wrap(<TimelinePanel />);

    // Not a silent short list — the reader is told what is missing.
    expect(await screen.findByText(/görevler/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/pages/marketing/command/TimelinePanel.test.tsx`
Expected: FAIL — cannot resolve `./TimelinePanel`

- [ ] **Step 3: Write the component**

```tsx
import { useQuery } from '@tanstack/react-query';
import { getHomeTimeline, type TimelineItem } from '../../../features/marketing/api/homeTimeline.service';
import { QueryStateBoundary } from '@/components/ui/QueryStateBoundary';
import { EmptyState } from '@/components/ui/EmptyState';

const KIND_LABEL: Record<TimelineItem['kind'], string> = {
  system: 'sistem',
  task: 'görev',
  appointment: 'randevu',
  campaign: 'kampanya',
};

const fmt = (iso: string) =>
  new Date(iso).toLocaleString('tr-TR', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' });

/**
 * Ne zaman ne yapılacak — dört kaynak tek eksende.
 *
 * `system` satırları kasten soluk: makinenin işi arka planda dönmeli, insanın
 * işiyle aynı ağırlıkta okunmamalı. Dördü aynı vurguyla dizilirse liste lapa
 * olur ve hiçbiri okunmaz.
 */
export function TimelinePanel() {
  const q = useQuery({
    queryKey: ['marketing', 'home', 'timeline'],
    queryFn: getHomeTimeline,
    refetchInterval: 60_000,
  });

  return (
    <QueryStateBoundary query={q}>
      {(data) => (
        <div className="flex flex-col gap-1">
          {data.unread.length > 0 && (
            <p className="text-xs text-amber-600 dark:text-amber-400 mb-1">
              Takvimin {data.unread.length} kaynağı okunamadı ({data.unread.join(', ')}) — bu liste eksik
            </p>
          )}
          {data.items.length === 0 ? (
            <EmptyState title="Planlanmış bir şey yok" />
          ) : (
            data.items.map((i) => (
              <div
                key={`${i.kind}-${i.id}`}
                data-testid={`tl-${i.kind}-${i.id}`}
                data-kind={i.kind}
                className={
                  i.kind === 'system'
                    ? 'flex gap-2 text-xs opacity-55 py-1'
                    : 'flex gap-2 text-sm py-1'
                }
              >
                <span className="tabular-nums opacity-60 shrink-0">{fmt(i.at)}</span>
                <span className="truncate">{i.title}</span>
                <span className="ml-auto text-[10px] uppercase tracking-wide opacity-40 shrink-0">
                  {KIND_LABEL[i.kind]}
                </span>
              </div>
            ))
          )}
        </div>
      )}
    </QueryStateBoundary>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/pages/marketing/command/TimelinePanel.test.tsx`
Expected: PASS, 2 tests

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/marketing/command/TimelinePanel.tsx frontend/src/pages/marketing/command/TimelinePanel.test.tsx
git commit -m "feat(home): calendar panel with recessive system rows and named missing sources"
```

---

### Task 6: Sekmeli sol sütun + rozet

**Files:**
- Create: `frontend/src/pages/marketing/command/LeftColumn.tsx`
- Test: `frontend/src/pages/marketing/command/LeftColumn.test.tsx`

Sekme seçildi (spec: C). Takvim ile akış aynı anda görünmediği için, akışta yeni bir başarısızlık olduğunda sekmenin kendini belli etmesi şart — yoksa seçimin bedeli körlük olur.

- [ ] **Step 1: Write the failing test**

```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { LeftColumn } from './LeftColumn';

vi.mock('./TimelinePanel', () => ({ TimelinePanel: () => <div>TAKVIM</div> }));
vi.mock('./AgentActivity', () => ({ AgentActivity: () => <div>AKIS</div> }));

describe('LeftColumn', () => {
  it('shows the calendar first and switches to the flow on click', () => {
    render(<LeftColumn failureCount={0} />);
    expect(screen.getByText('TAKVIM')).toBeInTheDocument();
    expect(screen.queryByText('AKIS')).not.toBeInTheDocument();

    fireEvent.click(screen.getByRole('tab', { name: /akış/i }));
    expect(screen.getByText('AKIS')).toBeInTheDocument();
  });

  it('badges the flow tab when something failed, so a tab you cannot see still reaches you', () => {
    render(<LeftColumn failureCount={3} />);
    expect(screen.getByTestId('flow-badge')).toHaveTextContent('3');
  });

  it('drops the badge when nothing has failed', () => {
    render(<LeftColumn failureCount={0} />);
    expect(screen.queryByTestId('flow-badge')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/pages/marketing/command/LeftColumn.test.tsx`
Expected: FAIL — cannot resolve `./LeftColumn`

- [ ] **Step 3: Write the component**

```tsx
import { useState } from 'react';
import { TimelinePanel } from './TimelinePanel';
import { AgentActivity } from './AgentActivity';
import { cn } from '@/components/ui/cn';

type Tab = 'timeline' | 'flow';

/**
 * Takvim ve akış aynı anda görünmez — sekmeli düzenin bilinçli takası.
 *
 * Bedeli körlük olmasın diye akış sekmesi başarısızlık sayısını rozet olarak
 * taşır: takvime bakarken bir iş düşerse sekme kendini belli eder. Rozetsiz bir
 * sekme, arkanda bir şey yanarken sana hiçbir şey söylemez.
 */
export function LeftColumn({ failureCount }: { failureCount: number }) {
  const [tab, setTab] = useState<Tab>('timeline');

  const tabCls = (t: Tab) =>
    cn(
      'text-[11px] uppercase tracking-wider pb-1 border-b-2 transition-colors',
      tab === t ? 'border-teal-500 opacity-100' : 'border-transparent opacity-50 hover:opacity-80',
    );

  return (
    <div className="flex flex-col h-full min-h-0">
      <div role="tablist" className="flex gap-4 mb-3 shrink-0">
        <button role="tab" aria-selected={tab === 'timeline'} className={tabCls('timeline')} onClick={() => setTab('timeline')}>
          Takvim
        </button>
        <button role="tab" aria-selected={tab === 'flow'} className={cn(tabCls('flow'), 'flex items-center gap-1.5')} onClick={() => setTab('flow')}>
          Akış
          {failureCount > 0 && (
            <span
              data-testid="flow-badge"
              className="inline-flex items-center justify-center min-w-[16px] h-4 px-1 rounded-full bg-red-500/15 text-red-500 text-[10px]"
            >
              {failureCount}
            </span>
          )}
        </button>
      </div>
      <div className="flex-1 min-h-0 overflow-y-auto">
        {tab === 'timeline' ? <TimelinePanel /> : <AgentActivity />}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/pages/marketing/command/LeftColumn.test.tsx`
Expected: PASS, 3 tests

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/marketing/command/LeftColumn.tsx frontend/src/pages/marketing/command/LeftColumn.test.tsx
git commit -m "feat(home): tabbed left column, with a badge so the hidden tab can still shout"
```

---

### Task 7: Ana ekranı iki sütuna böl

**Files:**
- Modify: `frontend/src/pages/marketing/command/CommandCenterPage.tsx`
- Modify: `frontend/src/pages/marketing/command/CommandCenterPage.test.tsx`

- [ ] **Step 1: Write the failing test**

`CommandCenterPage.test.tsx` içine ekle:

```tsx
it('renders two columns: the tabbed left panel and the chat', () => {
  renderPage(); // dosyadaki mevcut yardımcı
  expect(screen.getByTestId('home-left')).toBeInTheDocument();
  expect(screen.getByTestId('home-chat')).toBeInTheDocument();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/pages/marketing/command/CommandCenterPage.test.tsx`
Expected: FAIL — `home-left` bulunamadı

- [ ] **Step 3: Restructure the page**

`CommandCenterPage.tsx` içindeki dönen JSX'i şu iskeletle değiştir (mevcut `ApprovalQueue` sağ sütunun altında kalır — tam yetkili sohbet çoğu onayı ortadan kaldırır ama gerçekten kapılı kalan işler hâlâ bir yer ister):

```tsx
  const failureCount = useFailureCount(); // Task 9

  return (
    <div className="flex gap-4 h-[calc(100vh-8rem)] min-h-0">
      <section data-testid="home-left" className="basis-[38%] shrink-0 min-h-0">
        <Card className="h-full"><CardContent className="h-full pt-4 min-h-0">
          <LeftColumn failureCount={failureCount} />
        </CardContent></Card>
      </section>

      <section data-testid="home-chat" className="flex-1 min-w-0 min-h-0 flex flex-col gap-4">
        <Card className="flex-1 min-h-0"><CardContent className="h-full pt-4 min-h-0">
          <CommandBar />
        </CardContent></Card>
        <ApprovalQueue />
      </section>
    </div>
  );
```

Gerekli import'ları ekle: `import { LeftColumn } from './LeftColumn';`

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/pages/marketing/command/CommandCenterPage.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/marketing/command/CommandCenterPage.tsx frontend/src/pages/marketing/command/CommandCenterPage.test.tsx
git commit -m "feat(home): split the command centre into 38/62 columns"
```

---

### Task 8: Navigasyonu 3 yüzeye indir

**Files:**
- Modify: `frontend/src/features/marketing/navigation.ts`
- Modify: `frontend/src/features/marketing/navigation.test.ts`

Hiçbir rota silinmez — `App.tsx` dokunulmaz. Değişen yalnızca menüde neyin göründüğü; kaldırılan hub'ların sayfalarına doğrudan URL ve komut paleti ile erişilmeye devam edilir.

- [ ] **Step 1: Write the failing test**

`navigation.test.ts` içine ekle:

```ts
it('shows three surfaces plus settings, not fifteen hubs', () => {
  const hubs = visibleNav(NAV_HUBS, { isManager: true, isOwner: true, has: () => true, isAgency: false });
  expect(hubs.map((h) => h.id)).toEqual(['home', 'inbox', 'studio', 'settings']);
});

it('keeps every retired hub reachable by route, so nothing is lost', () => {
  const paths = NAV_HUBS.flatMap((h) => h.items.map((i) => i.path));
  for (const p of ['/leads', '/opportunities', '/calendar', '/tasks', '/invoices', '/automations']) {
    expect(paths).toContain(p);
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/features/marketing/navigation.test.ts`
Expected: FAIL — 15 hub dönüyor

- [ ] **Step 3: Regroup the hubs**

`navigation.ts` içinde `NAV_HUBS`'ı dört hub'a indir. Mevcut hub'ların `items` dizilerini **taşı**, silme:

- `home` — tek öğe: `/home`
- `inbox` — eski `inbox` + `contacts` + `sales` + `voice` öğeleri
- `studio` — eski `studio` + `growth` + `reports` + `prospecting` öğeleri
- `settings` — eski `settings` + `strategy` + `automation` + `payments` + `sites` + `memberships` + `agency` öğeleri

Her hub `tier: 'core'`. `feature:` alanlarına bu görevde **dokunma** — onlar ikinci spec'in işi.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/features/marketing/navigation.test.ts`
Expected: PASS

- [ ] **Step 5: Run the full frontend suite**

Run: `cd frontend && npx vitest run`
Expected: tüm testler PASS. Kırılan varsa hub id'sine göre gezinen bir testtir — yeni gruba göre güncelle.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/features/marketing/navigation.ts frontend/src/features/marketing/navigation.test.ts
git commit -m "feat(nav): fifteen hubs down to three surfaces plus settings"
```

---

### Task 9: Rozeti gerçek veriyle besle

**Files:**
- Create: `frontend/src/pages/marketing/command/useFailureCount.ts`
- Test: `frontend/src/pages/marketing/command/useFailureCount.test.ts`
- Modify: `frontend/src/pages/marketing/command/CommandCenterPage.tsx`

Task 7 rozeti sabit `0` ile bıraktı. Sabit sıfır, rozetin hiç çıkmaması demek —
yani sekmeli düzenin bedelini ödeyip telafisini almamak. Sekme ancak gerçek
sayıyı taşırsa körlüğü kapatır.

- [ ] **Step 1: Write the failing test**

```ts
import { renderHook, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { useFailureCount } from './useFailureCount';
import * as api from '../../../features/marketing/api/command.service';

const wrap = ({ children }: { children: React.ReactNode }) => {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return <QueryClientProvider client={qc}>{children}</QueryClientProvider>;
};

describe('useFailureCount', () => {
  it('counts runs whose tool calls failed', async () => {
    vi.spyOn(api, 'listAgentRuns').mockResolvedValue([
      { id: 'r1', goal: 'x', toolCalls: [{ ok: false }, { ok: true }] },
      { id: 'r2', goal: 'y', toolCalls: [{ ok: true }] },
      { id: 'r3', goal: 'z', toolCalls: [{ ok: false }] },
    ] as never);

    const { result } = renderHook(() => useFailureCount(), { wrapper: wrap });

    await waitFor(() => expect(result.current).toBe(2));
  });

  it('is zero when everything worked, so the badge stays off', async () => {
    vi.spyOn(api, 'listAgentRuns').mockResolvedValue([
      { id: 'r1', goal: 'x', toolCalls: [{ ok: true }] },
    ] as never);

    const { result } = renderHook(() => useFailureCount(), { wrapper: wrap });

    await waitFor(() => expect(result.current).toBe(0));
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/pages/marketing/command/useFailureCount.test.ts`
Expected: FAIL — cannot resolve `./useFailureCount`

- [ ] **Step 3: Write the hook**

```ts
import { useQuery } from '@tanstack/react-query';
import { listAgentRuns } from '../../../features/marketing/api/command.service';

/**
 * Akış sekmesinin rozeti: son koşularda kaç tanesi başarısız bir araç çağrısı
 * içeriyor.
 *
 * `AgentActivity` ile AYNI kaynağı okur, çünkü rozetin işaret ettiği liste
 * odur. Ayrı bir uçtan saymak, rozetin sekmeyle çelişebileceği anlamına gelir —
 * ve çelişen bir rozet, olmayan bir rozetten kötüdür.
 */
export function useFailureCount(): number {
  const { data } = useQuery({
    queryKey: ['marketing', 'agent-runs'],
    queryFn: () => listAgentRuns({ limit: 8 }),
    refetchInterval: 15_000,
  });
  return (data ?? []).filter((r) => r.toolCalls.some((c) => c.ok === false)).length;
}
```

`listAgentRuns`'ın imzası `command.service.ts`'te tanımlı; `limit` almıyorsa
çağrıyı oradaki imzaya uydur ve `AgentActivity`'nin kullandığı sorgu anahtarını
birebir kullan — iki bileşen aynı önbelleği paylaşmalı.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/pages/marketing/command/useFailureCount.test.ts`
Expected: PASS, 2 tests

- [ ] **Step 5: Wire it into the page**

`CommandCenterPage.tsx` içine `import { useFailureCount } from './useFailureCount';` ekle. `const failureCount = useFailureCount();` satırı Task 7'de zaten yerinde.

- [ ] **Step 6: Run the page test**

Run: `cd frontend && npx vitest run src/pages/marketing/command/`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add frontend/src/pages/marketing/command/useFailureCount.ts frontend/src/pages/marketing/command/useFailureCount.test.ts frontend/src/pages/marketing/command/CommandCenterPage.tsx
git commit -m "feat(home): feed the flow badge from the same runs the flow lists"
```


---

## Bitiş doğrulaması

- [ ] `cd backend && npx jest` — tüm birim testleri PASS
- [ ] `cd backend && E2E_REAL_DB=1 npx jest --config test/jest-e2e.json realdb --runInBand` — PASS
- [ ] `cd frontend && npx vitest run && npx tsc --noEmit` — PASS
- [ ] Etiketle ve dağıt: `git tag -a vX.Y.0 -m "home screen: calendar + flow + chat"` ve tag'i push et. Dağıtımı **tag'in kendi koşusuyla** doğrula — `gh run list --limit 1` önceki tag'in koşusunu döndürebilir.
- [ ] Canlıda `GET /marketing/home/timeline` 401 (token'sız) dönmeli; var olmayan bir alt yol 404 dönmeli — 401'in genel yakalayıcı olmadığının kanıtı.

## Sonraki planlar

1. **Geri alma** — spec'in "tam görünürlük + geri alma" sözleşmesinin ikinci
   yarısı. Bu plan görünürlüğü getiriyor (akış paneli + rozet), geri almayı
   **getirmiyor** ve bu bilinçli: geri alma, araç kaydından türetilen bir
   tersinirlik tablosu, her eylem için neyin geri alınacağının saklanması ve
   ayrı bir uç gerektiriyor — kendi planını hak edecek kadar iş. Sözleşmenin
   yarısıyla sevk ettiğimiz kayda geçsin: o gelene kadar tam yetkili sohbetin
   dengesi yalnızca görünürlüktür.
2. **İletişim + Kayıt birleşimi** — kişi birincil liste, iş kuyruğu filtreleri. Ayrı plan.
3. **Tek satın alma modeli** — `docs/superpowers/specs/2026-08-28-tek-satin-alma-modeli-design.md`. Bağımsız sevk edilebilir.
4. **Sohbetin uçtan uca doğrulanması** — AI kredisi geldiğinde. O zamana kadar sağ sütun arayüz olarak tamam, davranış olarak doğrulanmamış sayılır.
